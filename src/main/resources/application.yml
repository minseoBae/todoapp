spring:
  application:
    name: todoapp


  datasource:
    url: jdbc:mysql://lion-db.cby0e6s4csti.ap-northeast-2.rds.amazonaws.com:3306/mydatabase
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect